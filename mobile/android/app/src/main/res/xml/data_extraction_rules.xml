<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup/transfer rules.

  Nothing in this app is safe to copy off the device: session cookies and the
  biometric credential blob live in EncryptedSharedPreferences, whose keys are
  bound to this device's keystore. A restored copy on another device would be
  undecryptable at best and a credential leak at worst, so both cloud backup
  and device-to-device transfer are refused outright.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
